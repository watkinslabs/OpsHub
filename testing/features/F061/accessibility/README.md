# F061 accessibility lane

axe-core runs over the request dialog, request list, detail drawer, and the public recipient form at 320 px, plus keyboard-only completion, `fieldset`/`legend` row grouping, error wiring through `aria-describedby`, live-region announcements for draft and submit, and non-colour-only status. Gate `F061_FEATURE`.
