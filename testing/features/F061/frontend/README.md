# F061 frontend lane

Vitest + MSW component tests for the requester dialog, request list, detail drawer, and the public recipient form: scope picking, draft persistence in `localStorage`, partial progress, conflict handling, terminal screens for expired and cancelled links, and error states. Gate `F061_FEATURE`.
