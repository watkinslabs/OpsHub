# F061 requirements lane

Traceability index from every `FR-F061-NN` and `NFR-F061-NN` in `work/tickets/F061-update-requests.md` to the lane that proves it. No test code lives here; `cases.md` is the map and `cargo xtask validate-work` fails when a requirement id is missing from it.
