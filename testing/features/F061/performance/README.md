# F061 performance lane

Criterion benchmarks for the NFR-F061-01 budgets: 200×20 public scope read, 50-cell submission through the grid apply path, 10,000-request list, and the reminder claim scan over 100,000 schedule rows, plus a constant-time check on token lookup. Gate `F061_FEATURE`.
