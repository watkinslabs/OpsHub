# F039 api

Route, provider-boundary, retrieval, proposal, settings, and usage-limit tests for AI formulas/queries. Runs against the seeded tenant with the `recorded` and `stub` adapters; no live model call is possible.
