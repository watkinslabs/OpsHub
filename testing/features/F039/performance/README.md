# F039 performance

Criterion benchmarks for retrieval scope construction, envelope build and redaction, request and compile paths measured excluding provider latency, proposal apply, query read, expiry scan, and the pre-egress limit check.
