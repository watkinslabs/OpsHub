# File line-limit check

Hard rule: every repository file is ≤500 lines.

The CI check must scan source, tests, configuration, and documentation. Generated files may be excluded only when generated by a committed reproducible command and stored outside normal source paths.

Failure output must include the path and line count. Split oversized files by function, feature behavior, or responsibility.
